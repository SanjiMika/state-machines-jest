import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

import { dessertContext } from 'components/Desserts/dessertContext'

describe('state of form', () => {
  test('state of form', () => {

  });
})
